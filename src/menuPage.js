// Menu

function menuPage() {
    var sentenceMenu = document.createElement('h1');
    sentenceMenu.classList = 'sentence';
    sentenceMenu.textContent = "savor lunar's ever-changing menu, a nightly delight of culinary surprises.";

    return [sentenceMenu];
}

export {menuPage};